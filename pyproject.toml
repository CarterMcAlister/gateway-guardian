[project]
name = "gateway-guardian"
version = "0.1.0"
description = "Multi-profile Gateway Guardian supervisor for Hermes and OpenClaw."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
